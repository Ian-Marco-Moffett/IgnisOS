#include <win.h>
#include <stdio.h>
#include <desktop.h>
#include <draw.h>

void main(void) {
  // puts("Ayo\n");
  /*
  for (uint32_t y = 10; y < 200; ++y) {
    for (uint32_t x = 10; x < 200; ++x) {
      libgfx_putpix(x, y, 0xFF0000);
    }
  }
  */

  // kesswm_draw_desktop();
  // window_t welcome_window = kesswm_open_window(50, 50, 300, 300, "Welcome :-)");
  // kesswm_putstr(welcome_window, "Welcome to\nIgnisOS!");
  while (1);
}
